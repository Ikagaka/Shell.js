/// <reference path="../typings/index.d.ts"/>
import * as CacheCanvas from "./CacheCanvas";

QUnit.module('CacheCanvas');

QUnit.test('CacheCanvas.parseDescript', (assert)=>{
  console.log(CacheCanvas);
  assert.ok(true === true);
});
